# Neon Breach — Claude Code Rules

## Version number
The version is in `index.html` in the footer (`v1.4.0`).
**Bump the patch version (e.g. v1.4.0 → v1.4.1) in every commit that changes any file.**
Use minor version (v1.5.0) for new features, patch (v1.4.1) for fixes.
